[project]
name = "mcp-toolbox"
version = "0.1.0"
description = "toolbox of mcp servers for enhancing agent capabilities"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.14.1",
    "httpx>=0.28.1",
]
